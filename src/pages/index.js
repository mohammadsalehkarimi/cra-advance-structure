import Login from "./auth/Login.component";
import Register from "./auth/Register.component";
import Logout from "./auth/Logout.component";

export default {
  Login,
  Register,
  Logout,
};

// all of export at one file and import from /pages directory
